{
  "name": "autlaut",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension for text-to-speech using Kokoro TTS",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "kokoro-js": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
